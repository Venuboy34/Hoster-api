
Fastapi==0.104.1
uvicorn[standard]==0.24.0

# Pydantic (Data Validation & Settings)
pydantic==2.5.0
pydantic-settings==2.1.0

# DEPENDENCY FIX: Install email-validator for Pydantic
# This resolves the warning/issue seen in the logs for EmailStr validation.
email-validator

# Database (MongoDB)
motor==3.3.2
pymongo==4.6.0

# Authentication and Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0
httpx==0.25.2

# Background Tasks (Celery) and Message Broker (Redis)
redis==5.0.1
celery==5.3.4


